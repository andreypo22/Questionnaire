{
  "name": "questionnaire-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "server": "json-server db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.5"
  }
}
